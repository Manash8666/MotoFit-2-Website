import MotoFitSystem from '@/components/sections/MotoFitSystem';
import ServiceCatalog from '@/components/sections/ServiceCatalog';

export default function ServicesPage() {
    return (
        <main className="min-h-screen bg-[#050505]">
            <MotoFitSystem />
            <ServiceCatalog />
        </main>
    );
}
